package paa.modele;

/**
 * Représente le type d'action possible d'une case, qui peut être un déplacement, une capture (manger) ou une promotion.
 */
public enum TypeAction {
    DEPLACEMENT,
    MANGER,
    SPECIAL
}
